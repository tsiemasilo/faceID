import React from 'react'
import ReactDOM from 'react-dom/client'
import BiometricAttendance from './BiometricAttendance.tsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <BiometricAttendance />
  </React.StrictMode>,
)
